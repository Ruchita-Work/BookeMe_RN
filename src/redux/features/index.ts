import authSlice, { authActions } from "@redux/features/auth/authSlice";
import onboardingSlice, {
  onboardingActions,
} from "@redux/features/onboarding/onboardingSlice";

export { authSlice, authActions, onboardingActions, onboardingSlice };
